spring:
  datasource:
    url: jdbc:h2:mem:framegenesis_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

gemini:
  api:
    key: test-key
    model: gemini-2.5-flash

jwt:
  secret: framegenesis-test-secret-with-at-least-thirty-two-bytes
